version: "3"

services:

  app:
    container_name: app
    build: './app'

  mongo:
    container_name: mongo
    image: mongo
    restart: always
    volumes:
      - ./data:/data/db

  zeppelin:
    image: dylanmei/zeppelin
    ports:
      - 8080:8080
